apiVersion: v1
kind: Service
metadata:
  name: forgestatus-server-service-${ENV}
spec:
  type: NodePort
  selector:
    app: forgestatus-server-${ENV}
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
