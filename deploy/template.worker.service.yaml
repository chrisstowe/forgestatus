apiVersion: v1
kind: Service
metadata:
  name: forgestatus-worker-${WORKER_ID}-service-${ENV}
spec:
  type: NodePort
  selector:
    app: forgestatus-worker-${WORKER_ID}-${ENV}
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
